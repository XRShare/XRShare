<Plan>
1.  **Modify `Model.swift`**: Remove the code block within `loadModelEntity` that applies a default `SimpleMaterial` if `entity.model?.materials.isEmpty` is true. This will prevent overriding the materials potentially defined within the `Heart.usdz` file itself, allowing the model's intended textures and materials to be used.
</Plan>

<file path="/Users/alikara/Desktop/CPSC 599/XR-Anatomy-App-2 copy 2/Shared/Model.swift" action="modify">
  <change>
    <description>Remove fallback SimpleMaterial application to allow USDZ materials to load</description>
    <search>
===
                    entity.orientation = simd_quatf(angle: .pi/2, axis: SIMD3<Float>(1, 0, 0))
                }
                
                // Apply materials if not already applied
                if entity.model?.materials.isEmpty == true {
                    entity.model?.materials = [SimpleMaterial(color: .white, isMetallic: false)]
                }
                
                // Set initial scale based on model type
                if modelType.rawValue.lowercased() == "pancakes" {
                    entity.scale = [0.04, 0.04, 0.04] // Further reduced scale for pancakes
===
    </search>
    <content>
===
                    entity.orientation = simd_quatf(angle: .pi/2, axis: SIMD3<Float>(1, 0, 0))
                }
                
                // REMOVED: Fallback material application. Let the USDZ define its own materials.
                // if entity.model?.materials.isEmpty == true {
                //     entity.model?.materials = [SimpleMaterial(color: .white, isMetallic: false)]
                // }
                
                // Set initial scale based on model type
                if modelType.rawValue.lowercased() == "pancakes" {
                    entity.scale = [0.04, 0.04, 0.04] // Further reduced scale for pancakes
===
    </content>
  </change>
</file>